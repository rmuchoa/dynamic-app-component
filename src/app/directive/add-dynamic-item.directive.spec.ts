import { AddDynamicItemDirective } from './add-dynamic-item.directive';

describe('AddDynamicItemDirective', () => {
  it('should create an instance', () => {
    const directive = new AddDynamicItemDirective();
    expect(directive).toBeTruthy();
  });
});
